# ===========================================
# CONFIGURATION BACKEND SENASKANE
# ===========================================
# Copiez ce fichier en .env et remplissez les valeurs

# Configuration de la base de données MySQL
# Pour Railway: Utilisez une base MySQL Railway ou PlanetScale
# Pour Render: Utilisez une base externe (PlanetScale, Railway, etc.)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=senaskane_db
DB_PORT=3306

# JWT Secret (Générez-en un nouveau pour la production!)
# Utilisez: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_here

# Port du serveur (Railway et Render définissent automatiquement PORT)
PORT=3000
NODE_ENV=production
HOST=0.0.0.0

# Configuration Email - Option 1: Resend (RECOMMANDÉ pour production)
# Inscrivez-vous sur https://resend.com (gratuit: 100 emails/jour)
RESEND_API_KEY=re_your_api_key_here

# Configuration Email - Option 2: SMTP (Fallback)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_gmail_app_password_here

# Adresse d'expéditeur pour les emails
EMAIL_FROM="Senaskane <noreply@senaskane.com>"

# URL de l'application
APP_URL=https://your-app-url.com

# CORS - Origines autorisées (séparées par des virgules)
# Ajoutez votre URL Vercel ici après déploiement
ALLOWED_ORIGINS=https://baila-genea.vercel.app,https://your-custom-domain.com
